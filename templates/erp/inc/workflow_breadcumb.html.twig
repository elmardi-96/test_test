{#<div class="row">
    <div class="btn-group btn-breadcrumb btn-breadcrumb2">
        {% set count = 0 %}
        {% for key , place  in { "annuler":"Annulé" , "creer":"Créer" , "encours":"En cours " , "valider":"Validé" , "commander":"Commandé"}  %}
            {% if count >= 0 %}
                <a href="#" class="btn btn-default
                   {% if workflow_has_marked_place(devis, key)  %}
                       btn-breadcrumb2-active
                   {% endif %}
                   ">{{ place }}</a>
                {% set count = count + 1%}
            {% endif %}
            </button>
        {% endfor %}   
    </div> 
</div>#}




<div class="row">
    <div class="btn-group btn-breadcrumb btn-breadcrumb2">
        <a href="#" class="btn btn-default"><b>Demande client </b> </a>
       
    </div> 
</div>



