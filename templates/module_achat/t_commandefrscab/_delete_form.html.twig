<form method="post" action="{{ path('t_commandefrscab_delete', {'id': t_commandefrscab.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_commandefrscab.id) }}">
    <button class="btn">Delete</button>
</form>
