<form method="post" action="{{ path('univ_p_concourscab_delete', {'id': univ_p_concourscab.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ univ_p_concourscab.id) }}">
    <button class="btn">Delete</button>
</form>
