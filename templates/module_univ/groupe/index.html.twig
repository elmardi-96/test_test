{% extends 'base_univ.html.twig' %}
{% block title %}Configuration | Groupe & étudiant | UNIV
{% endblock %}
{% block body %}
	<div id="right-sidebar" class="actions animated"></div>

	<div class="row wrapper  white-bg page-heading  fixed-top-navigation-button">
		<div class="col-lg-4">
			<h2 class="fo">Univ , Division groupe
			</h2>
		</div>
		<div class="col-lg-8">
			<div class="title-action">
                <a href="{{path('divisiongroupe_new')}}" class="btn btn-white btn-xs" id="new_action"><i class="fa fa-pencil"></i> Ajouter </a>
			</div>
		</div>
		<div class="border-b"></div>
		<div class="col-lg-12">
			{{ wo_render_breadcrumbs() }}
		</div>
		{#{{ render(controller('App\\Controller\\moduleAchat\\CommandefrsController:showbreadcumb',{ 'commande': commande.id})) }}
		                                                                                                                                                                                                        #}

	</div>
	<br/><br/>


	<div class="row border-bottom white-bg dashboard-header mt-20">
		<div class="col-lg-12">

			<div class="ibox ">
				<div class="ibox-content" style="padding-top: 0px">
					<div class="table-responsive">
						<div class="row">
							<div
								class="col-2 list_actions_bloc" style="position: fixed ; display: none;" {#style="position: fixed;align-content: center;right: 50%;display: none;z-index: 9999999"#}>{#  <select class="select2_demo _3 form-control _multiple_actions">
																											                                        <option value="">Action</option>
																											                                        {% for key , operation in  operations  %}
																											                                            {% if operation.multiple %}
																											                                            <option value="{{operation.prefix}}">{{ operation.titre|capitalize }}</option>
																											                                       {% endif %}
																											                                            {% endfor %}
																											                                    </select>#}
							</div>
						</div>

						<table class="table  display table-striped table-bordered table-hover dataTables-example data-promotion" id="grid" style="width: 100%">
							<thead>
								<tr>
									<th>#</th>
									<th>Code</th>
									<th>Désignation</th>
									<th>Division</th>
								</tr>
							</thead>
							<tfoot>
								<tr>
									<th>#</th>
									<th>Code</th>
									<th>Désignation</th>
									<th>Division</th>
								</tr>
							</tfoot>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>

	{#{ render(controller('App\\Controller\\moduleAchat\\MaFacturefrscabController:showhistorique',{ 'facture': facture.id})) }#}

{% endblock %}


{% block javascripts %}
	{{ parent() }}
	{{ encore_entry_script_tags('univ_groupe') }}
{% endblock %}
