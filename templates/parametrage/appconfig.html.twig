{% extends 'base_config.html.twig' %}

{% block title %}Paramètrage index{% endblock %}

{% block body %}
    <div id="right-sidebar" class="actions animated" >

    </div>
   <div class="row wrapper  white-bg page-heading  fixed-top-navigation-button">


        <div class="col-lg-6">
            <h2 class="fo">Paramètrage<span>(app config)</span></h2>
        </div>

        <div class="border-b"></div>
        <div class="col-lg-12">
            {{ wo_render_breadcrumbs() }}
        </div>


    </div>
    <br/><br/>

    <div class="row border-bottom white-bg dashboard-header mt-20">
            <div class="col-lg-12">

								<div class="panel-heading">
									<div class="panel-options">																																																																																	                                        </ul>#}
										{% include './parametrage/inc/navigation.html.twig' %}
									</div>
								</div>
                <div class="ibox ">
                    <div class="ibox-content"> 
                        <div class="table-responsive">
                            <table class="table display table-striped table-bordered table-hover" id="grid-parametres" style="width: 100%" >
                                <thead>
                                    <tr>

                                        <th></th> 
                                        <th>Table </th>
                                        <th>Description </th>
                                        <th>Lien</th>

                                    </tr>
                                </thead>
                                <tbody>
                                    {% set i = 0 %}
                                    <tr>
                                        <td>{% set i = i + 1 %} {{ i }}</td>
                                        <td><a class="cd_op" href="{{path('us_module_index')}}">Module</a></td>
                                        <td>table de usmodule</td>
                                        <td>us_module</td>
                                    </tr>
                                    <tr>
                                        <td>{% set i = i + 1 %} {{ i }}</td>
                                        <td><a class="cd_op" href="{{path('us_sous_module_index')}}">Sous Module</a></td>
                                        <td>table de ussousmodule</td>
                                        <td>us_sous_module</td>
                                    </tr>
                                    <tr>
                                        <td>{% set i = i + 1 %} {{ i }}</td>
                                        <td><a class="cd_op" href="{{path('us_operation_index')}}">Operation</a></td>
                                        <td>table de usoperation</td>
                                        <td>us_operation</td>
                                    </tr>
                                    




                                </tbody>
                                <tfoot>
                                    <tr>

                                        <th></th> 
                                        <th>Table </th>
                                        <th>Description </th>
                                        <th>Lien</th>

                                    </tr>
                                </tfoot>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('config') }}
{% endblock %}  