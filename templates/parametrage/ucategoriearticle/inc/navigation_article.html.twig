{% set current_path = app.request.get('_route') %}
{#{{ dump(current_path) }}#}

<ul class="nav nav-tabs">
    {% if u_category.id  %}
    {% for operation in operations|sort((a, b) => a.ordr <=> b.ordr)%}
	
    {% if operation.horizontale  %}
    {% if operation.route is not null  %}
        <li>
           
           
            <a class="nav-link {% if  operation.route   in current_path  %} active {%endif%}" href="{{ path(operation.route , {id:u_category.id}) }}">
                <i class="fa {{ operation.icon }}"></i>
                {{ operation.titre|capitalize }}   </a>
        </li>
        
     
        {% endif %}
    {% endif %}
{% endfor %}


    {% else %}
        <li> <a class="nav-link active"  ><i class="fa fa-info-circle"></i> Informations</a></li>
        {% endif %}
</ul>
{#
<ul class="nav nav-tabs">
    
    

  


    
    
        <li>
            <a class="nav-link {% if "u_categorie_show" in current_path %} active {%endif%}" href="{{ path('u_categorie_show' ,{'id':u_category.id}) }}" ><i class="fa fa-info-circle"></i> Informations </a>
        </li>


        <li>
            <a class="nav-link {% if "stock_category_edit" in current_path %} active {%endif%}" href="{{ path('stock_category_edit' ,{'id':u_category.id}) }}" ><i class="fa fa-pencil"></i> Modifier Categorie
                
            </a>
        </li>

   
</ul>
#}