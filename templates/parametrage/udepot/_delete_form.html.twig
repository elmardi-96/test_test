<form method="post" action="{{ path('us_groupe_delete', {'id': us_groupe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ us_groupe.id) }}">
    <button class="btn">Delete</button>
</form>
