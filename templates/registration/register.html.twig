<!DOCTYPE html>
<html>

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>
        {% block title %}Inscription{% endblock %}
        </title>
        <link rel="shortcut icon" href="{{asset('/img/icon.png')}}" />
        {% block stylesheets %}
            {# 'app' must match the first argument to addEntry() in webpack.config.js #}
            {{ encore_entry_link_tags('app') }}

            <!-- Renders a link tag (if your module requires any CSS)
                 <link rel="stylesheet" href="/build/app.css"> -->
        {% endblock %}

</head>

<body class="gray-bg">

    <div class="middle-box text-center loginscreen   animated fadeInDown">
        <div>
            <div>
<img src="/img/logo.png" alt="image" class="feed-photo" style="margin-top: 5%;">
                

            </div>
            <h3>Register</h3>
            <p>Créez votre compte.</p>
            {{ form_start(form, { 'attr': {'class': 'm-t'} }) }}
                <div class="form-group">
                 {{ form_row(form.nom, { 'attr': {'class': 'form-control', 'placeholder':'Nom'} }) }}
                </div><br>
                <div class="form-group">
                 {{ form_row(form.prenom, { 'attr': {'class': 'form-control', 'placeholder':'Prénom'} }) }}
                </div><br>
                <div class="form-group">
                 {{ form_row(form.email, { 'attr': {'class': 'form-control', 'placeholder':'Email'} }) }}
                </div><br>
                <div class="form-group">
                {{ form_row(form.login, { 'attr': {'class': 'form-control', 'placeholder':'Username'} }) }}
                </div><br>
                <div class="form-group">
                {{ form_row(form.plainPassword.first) }}
                </div><br>
                <div class="form-group">
                {{ form_row(form.plainPassword.second) }}
                </div><br>
                {{ form_row(form.submit, { 'attr': {'class': 'btn btn-primary block full-width m-b'} }) }}

            {{ form_end(form) }}
            <p class="m-t"> <small>Inspinia we app framework base on Bootstrap 3 &copy; 2020</small> </p>
        </div>
    </div>

</body>

</html>
