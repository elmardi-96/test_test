{#<div id="page-wrapper" class="gray-bg">
	<div class="row">
		<nav class="navbar navbar-fixed-top" role="navigation">
			<div class="navbar-header">
				<a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="#">
					<i class="fa fa-bars"></i>
				</a>
				<div class="single-mdl">
					<img src="{{asset('img/modules/'~app.session.get('modules')['_stock']['image'])}}" class="rounded-circle circle-border m-b-md" alt="profile">
					<h2>
						{{ app.session.get('modules')['_stock']['titre']|upper }}
					</h2>
				</div>
			</div>
			<ul class="nav navbar-top-links navbar-right">
				{% include './inc/header_app_config_logout.html.twig' %}
			</ul>
		</nav>
	</div>#}





<div id="page-wrapper" class="gray-bg">
	<div class="row">


		<nav class="navbar navbar-fixed-top" role="navigation">
			<div class="navbar-header">
				<a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="#">
					<i class="fa fa-bars"></i>
				</a>

				<ul class="navbar-top-links navbar-top-dossier">
					<li class="nav-item">
						<a class="nav-link" href="{{ path('stock_dossiers_by_module',{'id':app.session.get('moduleCourantStock').id}) }}">
							<i class="fa fa-folder"></i>
							{% if (app.session.get('modules')[app.session.get('moduleCourantStock').prefix]['dossiers'][app.session.get('dossierCourantStock').prefix]  is defined) %}
								{%   set dossier = app.session.get('modules')[app.session.get('moduleCourantStock').prefix]['dossiers'][app.session.get('dossierCourantStock').prefix] %}
								{{ dossier.titre }}
								({{ (dossier.abreviation) }})
							{% endif %}
						</a>
					</li>
				</ul>
			</div>
			<ul class="nav navbar-top-links navbar-right">

				{% include './inc/header_app_config_logout.html.twig' %}


			


			</ul>


		</nav>


	</div>


